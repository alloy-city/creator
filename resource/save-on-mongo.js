export default function (doc) {
    /* DEPRECATED

    var url = `${apiDomain}/api/resource/`

    $.post(url, doc, function (_id) {
        doc._id = _id;
        Creator.resources.resources[_id] = doc;
        addResourceToDOM(doc, true);
        selectResource(_id);
    });
    */
}